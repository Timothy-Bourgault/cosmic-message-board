import { moonRating } from 'cosmic-message-board/helpers/moon-rating';
import { module, test } from 'qunit';

module('Unit | Helper | moon rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = moonRating([42]);
  assert.ok(result);
});
